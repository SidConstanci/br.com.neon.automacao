Projeto: AutomaçãoNeon

Este projeto consiste em um teste para ingressar na area de QA no banco neon.

Começando:
Para execultar o projeto, será necessário instalar os seguintes programas:

 - JDK 12.0.1: Nescessario para executar o projeto.
 - Maven 3.6.1: Nescessario para construir a build do projeto.
 - IntelliJ IDE: Para desenvolvimento e execução do projeto.

Desenvolvimento e execução:
Para iniciar o projeto, é nescessario clonar o projeto do GitHub num diretorio de sua preferencia:
    -cd "diretorio de sua preferencia"
    -git clone git@github.com:SidConstanci/br.com.neon.automacao.git

Contrução:
Para constuir o projeto com o Maven, execute os comando a baixo:
    -mvn clean install
O comando irá baixar todas as dependências do projeto e criar um diretorio target com os
artefatos construidos, que incluem o arquivo jar do projeto.

Configuração:
Para executar o projeto, é necessário utilizar o IntelliJ, para que o mesmo identifique as dependências
necessárias para a execução no repositório .m2 do Maven. Uma vez importado o projeto, será criado
um arquivo .classpath que irá informar qual a classe principal para a execução.

Teste:
Para rodar os testes, utilize o comando abaixo:
    -mvn test